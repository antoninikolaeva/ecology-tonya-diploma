import classes from './deviceClasses.json';
import link_types from './deviceLinkTypes.json';
import elements from './deviceElements.json';
import links from './deviceLinks.json';

const CLASSES = classes;
const LINK_TYPES = link_types;
const ELEMENTS = elements;
const LINKS = links;

export { CLASSES, LINK_TYPES, ELEMENTS, LINKS };